# Reference Handler

This directory contains a canonical HTTP handler example for the `webdev-go-react-psql` stack. It demonstrates a production-style handler with request struct, validation, authorization check, service call, error mapping, and test expectations.

The goal is to give implementer models a concrete pattern to copy, not a library to import.
