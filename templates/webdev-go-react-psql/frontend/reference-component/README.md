# Reference Component

This directory contains a canonical React/TypeScript component example for the `webdev-go-react-psql` stack. It demonstrates a form that consumes generated types, calls the API, and handles loading and error states.

Use it as the pattern for implementers building new feature components.
